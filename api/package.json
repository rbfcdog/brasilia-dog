{
  "name": "nextwave-stripe-mpp-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "validate:mpp": "mppx validate http://127.0.0.1:3000/paid",
    "fetch:profile": "node --env-file-if-exists=.env src/fetch-profile.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "mppx": "^0.9.1",
    "stripe": "^22.6.0"
  }
}
